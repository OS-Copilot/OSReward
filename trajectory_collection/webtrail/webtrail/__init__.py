"""webtrail: web trajectory collection for agent training data.

An async pipeline that drives vision-language agents through live websites,
records complete step-level evidence (screenshots, HTML, accessibility tree,
element maps, parsed actions), and ships with domain-aware pacing, block
detection, and rule-based post-collection filtering.
"""

__version__ = "0.1.0"
