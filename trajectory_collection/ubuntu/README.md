# Ubuntu Trajectory Collection

Placeholder. Planned: drive agents in Ubuntu desktop environments (e.g. VM- or
OSWorld-style setups) and record step-level evidence (screenshots, a11y trees,
actions) in the same stage layout as `../webtrail`.
