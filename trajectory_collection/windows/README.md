# Windows Trajectory Collection

Placeholder. Planned: drive agents in Windows desktop environments (e.g. VM- or
WindowsAgentArena-style setups) and record step-level evidence (screenshots,
UI trees, actions) in the same stage layout as `../webtrail`.
