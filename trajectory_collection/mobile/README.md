# Mobile Trajectory Collection

Placeholder. Planned: drive agents on Android (e.g. emulator- or
AndroidWorld-style setups) and record step-level evidence (screenshots, view
hierarchies, actions) in the same stage layout as `../webtrail`.
